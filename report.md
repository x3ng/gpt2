# GPT-2 117M 模型训练实验报告

## 1 实验目标

从头训练一个 GPT-2 规模的 Transformer 语言模型，参数规模为 117M，隐变量维度 768，12 层，12 头。基于 HuggingFace Transformers 框架实现，使用 OpenAI WebText 数据集和 WikiText-103 数据集进行训练。

## 2 模型架构

### 2.1 模型配置

采用 HuggingFace 的 GPT2LMHeadModel，从随机权重初始化。具体配置如下：

| 参数 | 值 |
|------|-----|
| 词汇表大小 (vocab_size) | 50,257 |
| 最大位置编码 (n_positions) | 1,024 |
| 上下文窗口 (n_ctx) | 1,024 |
| 隐变量维度 (n_embd) | 768 |
| Transformer 层数 (n_layer) | 12 |
| 注意力头数 (n_head) | 12 (每头维度 64) |
| 前馈层维度 | 3,072 (4 × 768) |
| 激活函数 | GELU |
| 参数量 | ~117M |

### 2.2 参数量分析

模型的主要参数来源如下：

| 组件 | 计算 | 参数量 |
|------|------|--------|
| 词嵌入 (Token Embedding) | 50,257 × 768 | 38.6M |
| 位置编码 (Position Embedding) | 1,024 × 768 | 0.79M |
| 12 层 Transformer | — | ~76M |
| 输出层 (LM Head) | 768 × 50,257 | 38.6M |

GPT-2 采用权重共享（weight tying）机制，输出层与词嵌入层共享同一组权重，因此实际参数量约为 117M，而非 154M。每层 Transformer 包含一个多头自注意力模块和一个前馈网络，各附带 LayerNorm 和残差连接。

## 3 训练数据

实验使用了两个数据集的混合语料。

### 3.1 OpenAI WebText

该数据集为 OpenAI 训练原始 GPT-2 时使用的网页文本数据，来源于 Reddit 上高赞链接对应的网页内容，涵盖新闻、科普、评论等多种文本类型。训练集（webtext.train.jsonl）大小 648MB，包含 250,000 条文本，总字符数约 6.54 亿；验证集（webtext.valid.jsonl）大小 13MB，包含 5,000 条文本。数据以 JSONL 格式存储在本地。

### 3.2 WikiText-103

该数据集由 Salesforce 发布，内容为维基百科的高质量英文文本。原始数据下载大小约 301MB，处理后数据集约 523MB，其中训练集包含 1,801,350 条文本（含大量短行和空行），验证集包含 3,760 条文本。相较于 WebText 的口语化风格，WikiText-103 的文本更为规范，包含大量完整的百科条目，有助于模型学习结构化知识和规范的书面表达。该数据集通过 HuggingFace Datasets 加载。

### 3.3 数据混合策略

两个数据集分别完成预处理后，按训练集和验证集分别拼接（concatenate），形成统一的训练语料。WebText 提供了多样化的网页文本基础，WikiText-103 作为补充增加了维基百科的高质量规范文本。

## 4 数据处理方法

### 4.1 分词

使用 GPT-2 原始的 BPE（Byte-Pair Encoding）分词器，词汇表大小 50,257。该分词器以字节级 BPE 算法对文本进行子词切分，能够覆盖任意 Unicode 字符，无需未登录词（OOV）处理。

### 4.2 序列构造

实验对比了两种序列构造方式：

**填充截断法（Padding + Truncation）**：将每条文本截断或填充至固定长度。该方法实现简单，但存在两个不足：一是短文本会引入大量无意义的填充 token（pad token），浪费计算资源；二是填充位置上的注意力计算和损失计算需要额外屏蔽处理。

**拼接分块法（Concatenate-and-Chunk）**：将所有文本的 token 序列首尾拼接为一条长序列，然后按固定长度等分切分。这种方式确保每个训练样本均为有效 token，不产生填充浪费；同时不同文档的边界自然出现在序列中间，模型需要学会在上下文中区分文档边界，这与实际使用场景更为一致。

首轮实验采用填充截断法，第二轮改用拼接分块法。

### 4.3 序列长度选择

GPT-2 的位置编码上限为 1,024（n_positions=1,024）。首轮实验将序列长度设为 128，这意味着模型训练时只使用了位置编码的前 128 个位置，后半段位置编码从未被训练，模型的注意力机制也无法学习到长距离依赖关系。第二轮将序列长度提升至 1,024，使模型在完整的上下文窗口内训练。

## 5 训练配置

### 5.1 超参数

两轮实验采用相同的优化器和学习率配置，主要差异在于序列长度、batch size 和评估频率：

| 配置项 | 首轮实验 | 第二轮实验 |
|--------|----------|------------|
| 训练数据 | WebText | WebText + WikiText-103 |
| 序列长度 (block_size) | 128 | 1,024 |
| 序列构造方式 | 填充截断法 | 拼接分块法 |
| 每设备 batch_size | 32 | 16 |
| 梯度累积步数 | 1 | 2 |
| 有效 batch_size | 32 | 32 |
| 学习率 | 5×10⁻⁴ | 5×10⁻⁴ |
| 学习率调度 | 线性 warmup + 线性衰减 | 线性 warmup + 线性衰减 |
| Warmup 步数 | 1,000 | 1,000 |
| 训练轮数 | 5 | 5 |
| 评估频率 | 每 50 步 | 每 500 步 |
| 保存频率 | 每 200 步 | 每 1,000 步 |
| 精度 | FP16 混合精度 | FP16 混合精度 |

### 5.2 配置调整说明

序列长度从 128 提升到 1,024 后，单步训练的显存消耗显著增加（注意力计算的复杂度为 O(n²)，序列长度翻倍时显存占用近似翻两番）。为适应显存限制，将每设备 batch_size 从 32 降至 16，同时设置梯度累积步数为 2，使得有效 batch_size 仍为 32，保持训练稳定性。

首轮实验每 50 步进行一次评估，在约 39,000 步的训练过程中产生了近 780 次评估，对训练效率影响较大。第二轮将评估间隔调整为 500 步，在保证监测训练过程的同时减少了评估开销。

## 6 实验结果与分析

### 6.1 首轮实验结果

使用 WebText 数据集，block_size=128，填充截断法，共训练 39,065 步：

| 指标 | 值 |
|------|-----|
| 初始训练损失 | 10.62 |
| 最终训练损失 | 3.64 |
| 最终验证损失 | 4.00 |
| 困惑度 (perplexity) | 54.6 |

### 6.2 第二轮实验结果

使用 WebText + WikiText-103 混合数据集，block_size=1024，拼接分块法，共训练 40,160 步。验证损失在关键节点的变化如下：

| 训练步数 | 验证损失 | 阶段说明 |
|----------|----------|----------|
| 500 | 6.21 | 训练初期 |
| 1,000 | 5.48 | warmup 结束 |
| 5,000 | 4.07 | 快速下降阶段 |
| 10,000 | 3.81 | — |
| 20,000 | 3.61 | 下降放缓 |
| 30,000 | 3.52 | 趋于收敛 |
| 40,000 | 3.47 | 最终最佳 |

最终指标：

| 指标 | 值 |
|------|-----|
| 初始训练损失 | 9.77 |
| 最终训练损失 | 3.24 |
| 最终验证损失 | 3.47 |
| 困惑度 (perplexity) | 32.2 |

### 6.3 两轮实验对比

| 指标 | 首轮 | 第二轮 | 变化幅度 |
|------|------|--------|----------|
| 验证损失 | 4.00 | 3.47 | -13.3% |
| 困惑度 | 54.6 | 32.2 | -41.0% |
| 训练损失 | 3.64 | 3.24 | -11.0% |

第二轮实验的验证损失从 4.00 降至 3.47，困惑度从 54.6 降至 32.2，改善显著。这一结果可归因于以下三个因素的共同作用：

1. **数据量增加**：加入 WikiText-103 后训练语料更加丰富，维基百科的高质量文本为模型提供了更规范的语言输入。
2. **序列长度提升**：从 128 提升至 1,024，模型能够在更长的上下文中建立 token 间依赖关系，且位置编码得到了充分利用。
3. **数据处理方式改进**：拼接分块法消除了填充浪费，每个训练 token 都是有效的，训练效率更高。

### 6.4 训练过程分析

从第二轮实验的损失变化来看，训练过程可分为三个阶段：

**快速下降期（0 ~ 5,000 步）**：验证损失从 6.21 迅速降至 4.07。模型在此阶段学习到基本的词汇共现规律和常见的语法结构。

**稳定下降期（5,000 ~ 25,000 步）**：验证损失从 4.07 缓慢降至 3.56。模型在持续优化，学习更细粒度的语义关系和长距离依赖。

**收敛期（25,000 ~ 40,160 步）**：验证损失从 3.56 降至 3.47，下降速度明显放缓。模型接近当前数据规模下的能力上限，进一步训练的边际收益减小。

首轮实验呈现出相似的三阶段模式，但各阶段对应的步数不同（因 block_size 差异导致总步数接近但每步覆盖的 token 数不同）。

### 6.5 泛化能力分析

首轮实验的训练损失（3.64）与验证损失（4.00）之间的差距为 0.36，第二轮实验的差距为 0.23。第二轮的 train-eval gap 更小，表明模型的泛化能力有所改善。这可能得益于数据量的增加和更合理的序列构造方式——拼接分块法使得训练样本中不同文档的自然边界成为模型需要处理的挑战，在一定程度上起到了正则化效果。

两轮实验均未观察到严重的过拟合现象，验证损失始终与训练损失保持同步下降。

### 6.6 与参考模型的比较

原始 GPT-2 Small 在约 40GB 文本上训练了更长时间，在 WikiText-103 上的困惑度约为 25~30。本实验的模型困惑度为 32.2，略高于原始 GPT-2，主要原因包括训练数据规模较小（WebText 约 648MB + WikiText-103 约 523MB，合计约 1.2GB，远小于原始 GPT-2 的 40GB）以及训练轮数有限。

## 7 推理测试

训练完成后，加载最终模型进行文本生成测试。生成过程采用采样生成策略，融合核采样与 Top-K 采样约束，设置温度系数为 0.8、Top-P 阈值为 0.9、Top-K 取值为 50；单次推理最大生成新 Token 长度设为 128，并引入重复惩罚系数 1.15 抑制文本冗余与循环重复问题。推理采用自动设备映射方式加载模型，依托硬件自动加速能力提升推理效率。

![推理结果展示](inference.png)

## 8 实验环境

| 项目 | 配置 |
|------|------|
| GPU | NVIDIA RTX 4090 (40GB) |
| Python | 3.12 |
| 深度学习框架 | PyTorch (CUDA) |
| 模型框架 | HuggingFace Transformers |
| 数据加载 | HuggingFace Datasets |
| HuggingFace 镜像 | hf-mirror.com |
| 第二轮训练耗时 | 约 7.8 小时 |

## 9 总结与讨论

本实验成功从头训练了一个 117M 参数的 GPT-2 语言模型，最终验证困惑度为 32.2。通过两轮实验的对比，验证了以下改进措施的有效性：增加训练数据量、提升序列长度至模型上下文窗口上限、采用拼接分块法替代填充截断法。这些改进使验证损失从 4.00 降至 3.47，困惑度降低了 41%。

当前模型的困惑度仍高于原始 GPT-2 Small（约 25~30），可能的改进方向包括：引入更大规模的训练语料（如 The Pile，约 825GB），以弥补当前训练数据仅约 1.2GB 的不足；增加训练轮数；尝试不同的学习率调度策略（如余弦衰减）；以及对 warmup 步数和峰值学习率进行更精细的调优。

---

实验环境：NVIDIA RTX 4090, Python 3.12, PyTorch + HuggingFace Transformers
